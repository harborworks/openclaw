{
  "name": "harbor-app",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "private": true,
  "devDependencies": {
    "@tsconfig/node22": "^22.0.1",
    "@types/aws-lambda": "8.10.148",
    "concurrently": "^9.1.2",
    "dotenv": "^16.4.7",
    "drizzle-kit": "^0.30.6",
    "sst": "^3.11.21"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspace @harbor-app/schema build && yarn workspaces run build",
    "clean": "yarn workspaces run clean && rm -rf node_modules",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "deploy": "sst deploy",
    "dev": "sst dev",
    "test": "yarn workspace @harbor-app/backend test && yarn workspace @harbor-app/frontend test && yarn workspace @harbor-app/daemon test"
  },
  "dependencies": {
    "@types/jsonwebtoken": "^9.0.10",
    "jsonwebtoken": "^9.0.3",
    "jwks-rsa": "^3.2.2"
  }
}
