services:
  database:
    image: "postgres:17.2-alpine"
    ports:
      - "54321:5432"
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - harbor-app-data:/var/lib/postgresql/data/
volumes:
  harbor-app-data:
