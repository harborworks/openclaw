##
## Harbor Dev stack — frontend with hot reload.
##
## Usage:
##   1. Copy frontend/.env.example to frontend/.env and fill in Cognito values
##   2. docker compose -f docker-compose.dev.yml up -d
##

services:
  frontend:
    image: node:22-bookworm-slim
    working_dir: /app
    command: sh -c "npm install && npm run dev"
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - frontend_node_modules:/app/node_modules
    env_file:
      - path: ./frontend/.env
        required: false
    environment:
      - CHOKIDAR_USEPOLLING=true
    restart: unless-stopped

volumes:
  frontend_node_modules:
